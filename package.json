{
  "name": "zagent",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:hub": "npm run dev -w @zagent/hub",
    "dev:web": "npm run dev -w @zagent/web",
    "typecheck": "tsc -p packages/protocol && tsc -p packages/hub && tsc -p packages/web",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
